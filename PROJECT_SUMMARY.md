# 🏗️ Civil News Hub: 프로젝트 종합 진행 현황 및 논의 내역 정리

> **📌 현재 버전**: `ver 1.0.31` (누적 수정 31회 반영 / 내부 관리 버전 / 웹 화면 비노출)  
> **버전 관리 규칙**: 수정 및 업그레이드 시마다 `+0.0.1` 자동 증가 (메이저 `1.0.0`, 마이너 `0.1.0`는 사용자 지시 시에만 변경)

본 문서는 **Civil News Hub(토목 뉴스 브리핑 & 채용·공모전 허브)**와 관련하여 지금까지 논의하고 구현한 모든 기능, UI 리디자인, 브랜치 작업 및 향후 로드맵을 체계적으로 정리한 종합 문서입니다.

---

## 📍 1. 관련 정리 문서 위치 안내

| 문서명 | 파일 위치 | 주요 내용 |
| :--- | :--- | :--- |
| **운영 및 개발 원칙** | [`GEMINI.md`](file:///C:/Users/최익석/Desktop/goofy-borg/GEMINI.md) | 팩트 기반 공모전 수집 규칙, 카테고리 탭 고정 규격, 모바일 무경계 원칙, 북마크 표준 |
| **프로젝트 종합 현황** | [`PROJECT_SUMMARY.md`](file:///C:/Users/최익석/Desktop/goofy-borg/PROJECT_SUMMARY.md) *(저장소)*<br>[`project_status_and_roadmap.md`](file:///C:/Users/최익석/.gemini/antigravity-cli/brain/51b5f7b7-721e-418b-b7f7-d5374e28aadd/project_status_and_roadmap.md) *(아티팩트)* | 지금까지 구현된 전체 기능, 상세 논의 내역, 브랜치 작업, 향후 로드맵 |
| **공모전 탭 시안 프리뷰** | [`contest_category_redesign_preview.md`](file:///C:/Users/최익석/.gemini/antigravity-cli/brain/51b5f7b7-721e-418b-b7f7-d5374e28aadd/contest_category_redesign_preview.md) | 카테고리 3가지 디자인 시안(A: 아이콘 칩, B: 미니멀 언더라인, C: 글래스모피즘) 비교 |
| **모바일 기사 뷰 프리뷰** | [`mobile_article_redesign_preview.md`](file:///C:/Users/최익석/.gemini/antigravity-cli/brain/51b5f7b7-721e-418b-b7f7-d5374e28aadd/mobile_article_redesign_preview.md) | 모바일 무경계(Borderless) 피드 및 대형 헤드라인 리디자인 비교 시안 |

---

## 📊 2. 지금까지 구현 및 논의 완료된 내역 (Completed Work)

```mermaid
mindmap
  root((Civil News Hub))
    SPA 단일 페이지 아키텍처
      상단 GNB 크기 및 고정 레이아웃
      3대 탭 무로딩 실시간 전환 (뉴스 | 채용 | 공모전)
      URL 해시 라우팅 및 딥링크 지원
      레거시 HTML 자동 리다이렉트
    모바일 피드 리디자인
      답답한 외곽 박스 테두리 제거 (Borderless 쾌적 피드)
      헤드라인 18~20px 엑스트라 볼드로 확대
      3줄 핵심 AI 브리핑 블루 불릿 및 가독성 개선
    카테고리 탭 개편 (시안 B 채택)
      시안 B (미니멀 언더라인 & 슬림 뱃지) 전면 채택
      뉴스, 채용, 공모전 전 사이트 100% 일괄 적용
      GEMINI.md에 불변 표준 규격으로 영구 고정
    모바일 스와이프 안정화
      상하 출렁임/높이 변동 방지 (높이 44px 완전 고정)
      좌우 이동 시 글씨 늘어남 원천 차단 (font-weight 600 고정)
      화면 끝 고무줄 브라우저 스트레치 차단 (overscroll none)
    상단 헤더 & 푸터 클린업
      '마지막 업데이트' 정보를 사이트 최하단 푸터로 이전
      상단 수집 기사/진행 공고/유효 공모전 건수 뱃지 전면 제거
      상단 3대 탭 제목 밑 부가 설명 문구 전면 제거 (미니멀 헤더)
    핵심 데이터 파이프라인
      초임 연봉/급여순 정렬 기능 (만원 단위 자동 환산)
      마감 임박 알림 클린업 (긴급 배너/점멸 애니메이션/토글 제거, 차분한 D-Day 유지)
      GitHub Actions 매일 아침 자동 크롤링 & Pages 자동 배포
```

---

### 🔍 상세 작업 및 논의 히스토리

#### 1) 모바일 기사 화면 무경계(Borderless) 및 대형 카드화
- **기존 문제**: 기사마다 둘러싸인 테두리 박스와 이중 패딩으로 인해 모바일에서 화면이 답답해 보임.
- **개편 완료**: 외곽 테두리(`border`)와 배경 패딩을 전면 배제하여 끊김 없는 네이티브 피드로 전환하고, 기사 헤드라인 크기를 18~20px 엑스트라 볼드로 키워 가독성을 극대화함.

#### 2) 카테고리 탭 디자인 개편 (시안 B 채택 및 전체 일괄 고정)
- **시안 제작**: 둔탁했던 알약 버튼 디자인을 대체하기 위해 3가지 시안(A: 비주얼 아이콘 칩, B: 미니멀 언더라인, C: 글래스모피즘) 제작 및 제안.
- **시안 B 전면 적용**: 애플/토스 스타일의 세련된 2px 언더라인 바 스타일(시안 B)을 선택하셨으며, 토목 뉴스, 채용 공고, 공모전 등 사이트 내 모든 카테고리 탭에 일괄 고정 적용 완료.

#### 3) 모바일 카테고리 좌우 스와이프 시 글씨 늘어남 & 높이 흔들림 원천 차단
- **기존 문제**: 모바일에서 카테고리를 좌우로 스와이프하거나 탭을 바꿀 때, 활성화된 탭 글씨가 굵어지며 좌우 폭이 늘어나 인접 탭을 밀어내고 위아래로 흔들리는 현상 발생.
- **해결 조치**:
  - 수평 스크롤 컨테이너 높이를 `44px`로 완전히 고정하여 상하 출렁임 차단.
  - 탭 활성/비활성 여부와 무관하게 모든 탭 및 뱃지의 폰트 두께를 `font-semibold`(`font-weight: 600 !important;`)로 영구 고정하여 **글씨 폭이 늘어나는 현상 완벽 제거**.
  - 수평 스크롤에 `overscroll-behavior: none !important;`를 적용하여 브라우저 끝단에서 글씨가 고무줄처럼 늘어나는 탄성 스트레치 차단.

#### 4) 상단 헤더 클린업 및 푸터 이전 (`feature/footer-last-updated`)
- **업데이트 정보 푸터 이전**: 상단 헤더에 있던 '마지막 업데이트' 정보를 사이트 맨 아래 푸터 설명 문구 하단(`마지막 업데이트: MM월 DD일 HH:MM`)으로 이동.
- **상단 통계 뱃지 전면 삭제**: 상단에 노출되던 `수집 기사: 0건`, `진행 공고: 0건`, `유효 공모전 0건`, `업데이트: 09월 09일 07:08 (실시간)` 뱃지 컨테이너를 모두 삭제.
- **상단 제목 밑 설명 문구 전면 삭제**: 각 탭의 제목 밑에 적혀 있던 불필요한 설명 글(`인터넷에 올라온 토목 관련 주요 기사들을...`, `공기업, 1군 대형 건설사...`, `국토교통부, 한국도로공사...`)을 전면 삭제하여, 메인 타이틀만 돋보이는 미니멀 헤더로 정리.
- **카테고리 헤더 '섹션' 뱃지 전면 제거**: 토목 뉴스 각 카테고리별 분할 영역 상단에 노출되던 불필요한 `[섹션]` 뱃지를 제거하여 `카테고리명`과 `총 N건` 카운트만 깔끔하게 표시되도록 정리.

#### 5) 전 페이지 좌우 너비 및 레이아웃 규격 통일 (토목 뉴스 표준 고정)
- **문제점**: 공모전 탭의 검색창 너비 축소(`max-w-xl`), 카테고리 탭 풀 블리드 음수 마진 누락, 카드 그리드 컬럼 수 차이(공모전 2열 vs 뉴스 3열)로 인해 탭 전환 시 좌우 길이가 맞지 않던 현상 발생.
- **해결 조치**:
  - **검색창**: 모든 탭의 검색창을 `w-full` 100% 풀 와이드 규격으로 통일.
  - **카테고리 탭 스티키 바**: 화면 좌우 끝까지 뻗는 풀 블리드 음수 마진(`-mx-4 sm:-mx-6 lg:-mx-8 px-4 sm:px-6 lg:px-8`) 구조로 전 탭 통일.
  - **필터 및 정렬 컨트롤 바**: 카테고리 탭 바로 아래 `flex justify-between` 구조로 배치 통일.
  - **카드 그리드**: 모바일 1열, 태블릿 및 데스크탑 2열(`grid grid-cols-1 md:grid-cols-2 gap-5 sm:gap-6`)로 전면 통일.
  - [`GEMINI.md`](file:///C:/Users/최익석/Desktop/goofy-borg/GEMINI.md)에 불변 원칙(규칙 ⑥)으로 영구 등록.

#### 6) 마감 임박 알림 & 긴급 배너 전면 정리 (UI 클린업)
- **요청 사항**: "마감 임박 관련된 알림들 다 지워줘"
- **조치 내역**:
  - 채용 및 공모전 상단의 🚨 마감 임박 긴급 배너(`jobUrgentBannerContainer`, `contestUrgentBannerContainer`) 전면 삭제.
  - 필터 바의 `🚨 마감임박(D-3)만 보기` 퀵 토글 버튼 전면 제거 (정렬 드롭다운의 표준 마감순 정렬은 정상 유지).
  - 카드 및 상세 모달의 D-Day 배지에서 시각적 불안감을 주던 점멸(`animate-pulse`)과 불꽃(`flame`) 아이콘 제거, 차분하고 일관된 정적 D-Day 배지로 변경.

#### 7) 공모전 카드 '상세 요강 보기' 버튼 삭제 및 보상(상금/포상) 뱃지 배치
- **요청 사항**: "공모전 페이지에서 상세 요강 보기 버튼은 지우고 그 자리에 공모전 보상 그런거 적어주라"
- **조치 내역**:
  - 카드 하단 좌측의 불필요한 텍스트 버튼 `상세 요강 보기` 삭제 (카드 전체 클릭 시 상세 모달이 열리므로 중복 제거).
  - 해당 위치에 공모전 보상 뱃지(`🏆 ${contest.prize}`)를 깔끔한 앰버 틴트 뱃지로 배치하여 핵심 시상/포상 혜택을 한눈에 파악할 수 있도록 개선.
  - 카드 본문 영역의 중복 상금 박스를 제거하여 설명 문구의 가독성과 전체 카드 균형 개선.

#### 8) 공모전 카드 '공식 접수처' 버튼 문구를 '바로가기'로 간소화
- **요청 사항**: "공모전 페이지에서 공식 접수처 라는 버튼을 바로가기 로 바꿔줘"
- **조치 내역**:
  - 공모전 카드 하단 우측 외부 링크 버튼 레이블을 `공식 접수처`에서 직관적이고 간결한 `바로가기`로 변경.
  - 버튼 폭 축소를 통해 좌측 공모전 보상 뱃지의 표시 공간 확보 및 모바일 뷰 최적화.

#### 9) 전 페이지 카드 그리드 2열 레이아웃 개편 및 카드 사이즈 대형화
- **요청 사항**: "모든 페이지에서 지금 좌우 간격 그대로 하고 2줄씩 바꾸면서 사이즈 키워줘"
- **조치 내역**:
  - **좌우 너비 및 규격 100% 보존**: 토목 뉴스 표준인 `max-w-7xl`, `px-4 sm:px-6 lg:px-8` 규격을 그대로 유지.
  - **2열 그리드(2줄씩) 전환**: 토목 뉴스, 채용 공고, 공모전의 모든 카드 컨테이너를 기존 3열(`lg:grid-cols-3 gap-4 sm:gap-5`)에서 **2열(`grid-cols-1 md:grid-cols-2 gap-5 sm:gap-6`)**로 통일 변경.
  - **카드 내부 스케일 및 타이포그래피 비례 확대 ("사이즈 키워줘")**:
    - **카드 내부 여백**: `p-4 sm:p-5` -> `p-5 sm:p-7 rounded-2xl sm:rounded-3xl`로 쾌적한 카드 볼륨감 확보.
    - **헤드라인 타이틀**: 뉴스(`text-xl sm:text-2xl font-extrabold`), 채용 & 공모전(`text-base sm:text-xl font-bold`)로 시원한 가독성 부여.
    - **본문 및 요약**: 3줄 핵심 AI 요약 박스(`p-4 sm:p-5`, `text-sm sm:text-[15px]`), 채용 정보 및 공모전 요약 텍스트(`text-xs sm:text-sm`) 확대.
    - **뱃지 및 액션 버튼**: 카테고리/상태/급여 뱃지 및 공유/북마크/원문 바로가기 버튼 크기 비례 확대.

#### 10) 북마크 페이지 명칭을 '마이페이지'로 변경 (UI 네이밍 개편)
- **요청 사항**: "북마크 페이지 이름을 마이 페이지 로 바꾸면 좋겠어"
- **조치 내역**:
  - **헤더 퀵 버튼**: 상단 헤더 우측 북마크 버튼 문구를 `북마크 (N)`에서 `마이페이지 (N)`으로 변경.
  - **모바일 하단 탭바**: 4번째 탭 레이블을 `북마크`에서 `마이페이지`로 변경.
  - **섹션 타이틀 및 건수 알림**:
    - 뉴스: `마이페이지 · 저장한 기사`, `⭐ 마이페이지 기사 총 N건`
    - 채용: `마이페이지에 저장된 채용 공고`, `⭐ 마이페이지 채용 공고 총 N건`
    - 공모전: `마이페이지에 저장된 공모전`, `⭐ 마이페이지 공모전 총 N건`
  - **토스트 알림**: 진입 시 `🔖 마이페이지로 이동했습니다. (저장한 항목 모아보기)`로 직관적인 피드백 제공.

#### 11) 통합 마이페이지(My Page) 구축 - 전체 저장 항목 섹션별 일괄 조회
- **요청 사항**: "마이 페이지를 각각의 페이지들을 저장하는게 아니라 모든 글들을 저장한걸 한번에 볼 수 있게 만들어줘 각각의 페이지 별로 섹션 따로 쓰게 하고"
- **기존 한계**: 기존에는 현재 활성화된 탭 내부에서만 북마크 필터링이 동작(뉴스 탭에서는 저장된 뉴스만, 채용 탭에서는 저장된 채용만 표시)하여, 플랫폼 전체에서 사용자가 북마크한 항목들을 한자리에서 조망할 수 없었음.
- **개편 및 구현 내역**:
  1. **독립 통합 뷰(`tabPanelMyPage`) 신설**:
     - 상단 헤더 `마이페이지 (N)` 버튼 또는 모바일 하단 네비게이션 4번째 탭 클릭 시, 전역 독립 뷰(`switchMainTab('mypage')`, 해시 `#mypage`)로 매끄럽게 전환.
  2. **3대 카테고리 분리 섹션 구성**:
     - 📰 **저장한 토목 뉴스**: 북마크한 기사들을 2열 와이드 그리드로 렌더링.
     - 💼 **저장한 채용 공고**: 북마크한 채용 공고들을 2열 와이드 그리드로 렌더링.
     - 🏆 **저장한 공모전**: 북마크한 공모전들을 2열 와이드 그리드로 렌더링.
     - 각 섹션 헤더에 고유 아이콘, 타이틀, 건수 뱃지, 해당 탭으로 즉시 이동 가능한 `[바로가기 &rarr;]` 버튼 배치.
  3. **통계 뱃지 및 빈 상태(Empty State) 고도화**:
     - 상단 헤더에 `뉴스 N`, `채용 N`, `공모전 N`, `총 N건` 요약 뱃지 표시.
     - 특정 카테고리가 비어있을 때는 해당 카테고리 둘러보기 버튼이 포함된 경량 안내창 노출.
     - 전체 저장 항목이 0건일 경우 전체 둘러보기를 안내하는 대형 Empty State 표시.
  4. **GEMINI.md 규격 100% 준수**:
     - 최외곽 `max-w-7xl`, `px-4 sm:px-6 lg:px-8` 컨테이너 및 2열 카드 그리드(`grid-cols-1 md:grid-cols-2 gap-5 sm:gap-6`) 일관성 유지.
  5. **모바일 시뮬레이터(`mobile.html`) 연동**:
     - 상단 컨트롤 바에 `마이페이지` 스위처 버튼 추가 및 실시간 iframe 탭 전환 연동.

#### 12) 토목 뉴스 섹션 배경 컨테이너 제거 및 마이페이지 스타일 통일 (클린 UI 리팩토링)
- **요청 사항**: "토목 뉴스 섹션도 마이 페이지 섹션처럼 깔끔하게 해줘 뒤에 색깔 이런거 없는게 훨씬 괜찮다"
- **기존 문제**: 토목 뉴스 각 분야별 섹션에 `sm:bg-white dark:sm:bg-slate-900 border` 박스 배경 컨테이너가 둘러싸여 있어, 개별 뉴스 카드 뒤에 불필요한 이중 박스 배경이 생기는 답답함과 시각적 노이즈가 발생함.
- **개편 내역**:
  1. **배경 박스 및 테두리 전면 제거**: 섹션을 감싸던 외곽 배경(`bg-white/slate-900`), 외곽 테두리(`border`), 패딩(`p-7`), 그림자(`shadow-xs`)를 전면 배제하여 페이지 배경 위로 카드가 바로 놓이도록 변경.
  2. **마이페이지 섹션 헤더 규격 100% 동일 적용**:
     - 카테고리별 컬러 아이콘 뱃지 박스(`w-8 h-8 rounded-xl`)
     - 엑스트라 볼드 섹션 타이틀(`text-lg sm:text-xl font-extrabold`)
     - 깔끔한 건수 뱃지(`text-xs px-2.5 py-0.5 rounded-full`)
     - 정갈한 하단 경계선(`border-b border-slate-200/80 dark:border-slate-800 pb-3.5 mb-5 px-1`)
  3. **검색 결과 및 마이페이지 모드 포함 일괄 적용**: 기본 5개 카테고리뿐만 아니라 검색 결과 섹션 등 모든 뉴스 섹션을 마이페이지와 동일한 무경계 클린 섹션으로 통일.

#### 13) 공모전 카드 버튼 문구 '접수'로 간소화 및 세로 줄바꿈 방지 (모바일 레이아웃 최적화)
- **요청 사항**: "모바일 뷰에서 보면 공모전 바로가기 버튼이 세로로 써져서 별로 안이쁜데 어떤 방법이 좋을까"
- **원인 분석**: 모바일 좁은 화면 폭(340~380px)에서 좌측의 긴 보상(상금) 뱃지와 우측의 3개 액션 버튼(공유, 북마크, 바로가기)이 부딪히며 `whitespace-nowrap`과 `flex-shrink-0`이 없어 4글자 '바로가기'가 세로로 떨어져 깨지는 현상 발생.
- **해결 조치**:
  1. **2글자 직관적 액션 단어 통일**: 뉴스(`원문`), 채용(`지원`)과 동일한 2글자 규격인 **`접수`**로 간소화하여 여유 폭을 확보하고 전체 탭의 액션 버튼 레이블 통일성 달성.
  2. **세로 줄바꿈 원천 차단**: 버튼 및 우측 버튼 그룹에 `whitespace-nowrap flex-shrink-0`을 강제하여 어떤 좁은 화면에서도 가로 한 줄 온전한 형태 유지.
  3. **보상 뱃지 유연한 말줄임**: 좌측 보상 뱃지에 `flex-1 min-w-0 truncate`를 적용하여 텍스트가 긴 경우 자연스러운 말줄임(`...`) 처리.

#### 14) 프로젝트 내부 버전 관리 체계 수립 및 누적 13회 수정 반영 (ver 1.0.13)
- **요청 사항**: "내가 지금까지 수정한거 합산해서 버젼에 더해볼래"
- **운영 원칙 등록 및 버전 갱신**:
  1. **웹 화면(UI) 비노출**: 사용자에게 불필요한 시각적 노이즈를 주지 않기 위해 웹 화면에는 버전을 표시하지 않고 내부 관리용으로만 추적.
  2. **누적 수정 합산 반영**: 지금까지 사용자의 지시로 성공적으로 완료된 총 13회의 기능 개선 및 UI 리디자인 작업 내역(`13 × 0.0.1 = +0.0.13`)을 정밀하게 소급 합산하여 **`ver 1.0.13`**으로 갱신 확정.
  3. **향후 증가 규칙**: 다음 작업부터 사용자의 수정 요청을 완료할 때마다 자동으로 `+0.0.1`씩 증가 (`ver 1.0.14` &rarr; `ver 1.0.15` ...).
  4. **마이너/메이저 버전 고정**: 마이너(`0.1.0`) 및 메이저(`1.0.0`) 버전은 사용자의 직접 지시가 있을 때만 증가.
  5. **규칙 문서화**: [`GEMINI.md`](file:///C:/Users/최익석/Desktop/goofy-borg/GEMINI.md) 제4조 및 본 문서에 영구 원칙으로 기록.

#### 15) 3단계 종합 기능 고도화 패키지 (ver 1.0.14)
- **요청 사항**: "1단계에서 오늘의 토목 핵심 키워드(#) 필터 칩 제외하고는 진행하지 말고 2단계 3간계 전부 진행해줘"
- **구현 및 고도화 내역**:
  1. **1단계 - 오늘의 토목 핵심 키워드(#) 필터 칩**:
     - 상단 검색창 바로 아래 가로 스크롤형 핵심 키워드 칩 바 신설 (`#스마트건설`, `#GTX`, `#지하철`, `#고속도로`, `#교량`, `#철도`, `#수자원`, `#안전`, `#친환경`, `#AI`, `#BIM`).
     - 클릭 시 즉시 해당 키워드로 기사 필터링 및 활성화 토글 연동, 원클릭 초기화 지원.
  2. **2단계-1 - 채용 및 공모전 자격 뱃지 세분화**:
     - **채용 공고**: 기업분류 외에 2번째 행에 경력 자격(`신입`/`경력`), 근무지(`수도권`/`전국·현장`), 최종학력 뱃지를 추가하여 스펙 맞춤 검색성 강화.
     - **공모전**: 2번째 행에 참가대상 자격(`🎓 대학(원)생`, `🚀 스타트업·기업`, `👥 전 국민 누구나`, `💼 일반·전문가`) 뱃지 추가 및 상단 컨트롤 바에 참가대상 필터 칩 바(`전체`, `대학(원)생`, `스타트업·기업`, `전 국민`) 신설.
  3. **2단계-2 - 북마크 항목별 나만의 한 줄 개인 메모 (LocalStorage)**:
     - 뉴스, 채용, 공모전 각 카드의 하단 및 마이페이지 통합 뷰에 개인 메모 작성 및 인라인 확인 컴포넌트 탑재.
     - 전용 모달(`#bookmarkNoteModal`)을 통해 최대 200자의 개인 메모를 손쉽게 저장, 수정, 삭제 가능 (`civil_bookmark_notes` 로컬스토리지 영구 보관).
  4. **2단계-3 - 공모전 마감 일정 캘린더(달력) 뷰 모달**:
     - 공모전 탭에 `[📅 마감 캘린더]` 버튼 신설 및 풀 월간 캘린더 모달(`#contestCalendarModal`) 탑재.
     - 월별 달력 그리드에 일자별 마감 공모전 건수 및 제목 라벨 시각화, 이전/다음 달 탐색, 하단 이번 달 마감 공모전 타임라인 목록 및 클릭 시 상세 모달 원클릭 연동.
  5. **3단계-1 - PWA (프로그레시브 웹 앱) 및 오프라인/홈 화면 추가 지원**:
     - `manifest.json`, `sw.js` (서비스 워커) 탑재로 스마트폰 홈 화면 추가 시 네이티브 앱처럼 풀스크린(`standalone`) 구동.
     - Stale-While-Revalidate 캐싱 전략으로 빠른 초기 로딩 속도 보장.
  6. **3단계-2 - 기사 읽음(Read) 상태 체크 및 딤드 시각화**:
     - 기사 클릭(원문 이동) 시 자동으로 `✓ 읽음` 상태로 등록 (`civil_read_articles` 로컬스토리지 보관).
     - 이미 읽은 기사는 은은한 딤드(`opacity-75`) 처리 및 뱃지 표시로 정보 탐색 피로도 대폭 경감. 뱃지 클릭으로 읽음/안읽음 토글 지원.
  7. **3단계-3 - 첫 화면 시작 탭(뉴스/채용/공모전/마이페이지) 자동 기억 및 복원**:
     - 사용자가 마지막으로 머무른 탭을 `civil_last_tab`에 자동 저장하고, 재접속 또는 PWA 실행 시 해당 탭으로 즉시 자동 복원.

#### 16) 아침 7시 크롤링 기반 당일 대표 트렌딩 키워드 동적 추출 및 배치 (ver 1.0.15)
- **요청 사항**: "키워드를 아침 7시에 크롤링 할때 좀 대표적인 것들 몇개만 추려서 키워드 딸 수 있어? 그렇게 딴 키워드를 배치시키는거야 지금처럼 고정 키워드가 있지 않고"
- **기존 한계**: `#스마트건설`, `#GTX`, `#수자원` 등 고정된 키워드 배열만 표시되어, 당일 실제로 어떤 토목 현안이나 대형 인프라 프로젝트가 화제인지 반영되지 못함.
- **개편 및 구현 내역**:
  1. **수집기(`scraper.py`) 내 트렌딩 키워드 자동 추출기(`extract_trending_keywords`) 신설**:
     - 매일 아침 7시 GitHub Actions 크롤링 시 수집된 전체 기사(200여 건 이상)의 제목과 요약을 심층 분석.
     - 불용어(일반 단어, 정치·행정 노이즈 등)를 정밀 필터링하고, 빈도수와 토목 도메인 가중치를 결합하여 당일 가장 화제가 되는 대표 키워드 8~10개를 실시간 추출 (예: `#지천댐`, `#싱크홀`, `#태그리스`, `#무선제어`, `#항만공사`, `#도로공사`, `#철도망`, `#국토부`, `#도시철도`, `#AI` 등).
     - 추출된 키워드를 `data/news.json`의 `trending_keywords` 필드로 자동 저장.
  2. **프론트엔드(`static/app.js`) 동적 연동**:
     - 고정 키워드 대신 `news.json`에서 당일 대표 키워드 배열을 동적으로 수신하여 칩 바로 렌더링.
     - 라벨을 `<i data-lucide="trending-up"></i> 오늘의 키워드:`로 고도화하여 신선한 정보임을 시각화.
     - 키워드 클릭 시 해당 이슈 기사들만 즉시 필터링되며, 재클릭 시 원클릭 해제 지원.

#### 17) 전방위 체감 성능 및 렌더링 렉 원천 차단 최적화 (ver 1.0.16)
- **요청 사항**: "지금 상태에서 페이지 사용하는데 있어서 렉이 걸릴만한 부분이나 문제가 되는 부분 있으면 나한테 말하고 어떻게 하면 더 빠르고 좋아지는지 말해줘" &rarr; "오케이 일괄 적용해줘"
- **기존 병목 원인**:
  1. 뉴스, 채용, 공모전 검색창에 디바운스가 없어 한글 타이핑 1글자마다 10~15회 불필요한 전체 DOM 재생성 발생.
  2. `lucide.createIcons()`가 호출될 때마다 화면 전체를 풀스캔하여 CPU 연산 과부하 발생.
  3. 검색/키워드 결과 수십 개 카드가 페이징 없이 한꺼번에 DOM에 쏟아져 모바일 스크롤 프레임 드랍 유발.
  4. CDN `@latest` 조회로 인한 302 리다이렉트 통신 지연.
- **개편 및 최적화 내역**:
  1. **검색창 180ms 디바운스(Debounce) 전면 적용**: 뉴스(`newsSearchInput`), 채용(`jobSearchInput`), 공모전(`contestSearchInput`) 모두 디바운스를 적용하여 타이핑 중 불필요한 연산 90% 이상 차단 및 즉각적인 부드러운 입력 반응성 확보.
  2. **Lucide 아이콘 컨테이너 국소 스코핑(`safeCreateIcons`)**: 전체 DOM 풀스캔을 제거하고, 갱신된 컨테이너 내부만 타겟팅 렌더링하여 CPU 점유율 및 리플로우 80% 절감.
  3. **검색 및 키워드 결과 점진적 페이징(`SEARCH_PAGE_SIZE = 12`)**: 대량의 카드가 한 번에 DOM에 들어가는 것을 방지하고 12개 단위 페이징 및 `[결과 더보기 (+N개)]` 버튼 탑재.
#### 18) 토목 뉴스 무한 로딩 해결 및 아이콘 렌더러 재귀 버그 긴급 패치 (ver 1.0.17)
- **발생 원인 분석**:
  1. `static/jobs.js` 및 `static/contests.js`에서 전역 `safeCreateIcons` 함수가 브라우저 전역 스코프에서 선언되면서 `window.safeCreateIcons`를 자기 자신으로 덮어씀.
  2. 함수 내부의 `if (window.safeCreateIcons) window.safeCreateIcons(rootEl)` 조건이 무한 재귀 호출을 유발하여 `RangeError: Maximum call stack size exceeded` 발생.
  3. 이로 인해 초기화 이벤트(DOMContentLoaded) 도중 스크립트 실행이 중단되어 `loadNewsData()`가 정상 완료되지 못하고 `newsLoadingIndicator`가 계속 화면에 잔존(무한 로딩 현상).
- **수정 및 안정화 내역**:
  1. **전역 아이콘 렌더러 분리 및 재귀 원천 차단**: `static/app.js`에서 안전한 단일 `window.safeCreateIcons`를 정의하고, `jobs.js`(`renderJobIcons`) 및 `contests.js`(`renderContestIcons`)는 독립적인 네임스페이스 함수로 분리하여 상호 충돌 및 자기 재귀 원천 방지.
  2. **뉴스 로딩 예외 안전성 강화**: `loadNewsData` 내 정적 파일 폴백(`/data/news.json`) 시 응답 상태 검증 추가 및 예외 발생 시에도 `finally` 블록에서 로딩 스피너를 무조건 내리고 빈 상태(Empty State)로 안전 전환되도록 보강.
  3. **Lucide UMD 직접 로드**: `https://unpkg.com/lucide@0.441.0/dist/umd/lucide.min.js` 직접 로드로 불필요한 301 리다이렉트 완전 제거 및 캐시 버스터(`?v=20260911_0245`) 일괄 갱신.

#### 19) 토목 뉴스 카테고리 탭 단독 필터링 전환 및 네비게이션 개편 (ver 1.0.18)
- **요청 사항**: "토목뉴스 섹션들 채용 공고문 카테고리처럼 누르면 그 카테고리만 보이게 싹 바꿔줘"
- **기존 방식**: 5개 카테고리가 한 페이지에 세로로 모두 나열되어 있었으며, 탭 클릭 시 해당 섹션 위치로 스크롤만 이동하여 다른 카테고리가 여전히 노출됨.
- **개편 내역 (채용 공고문과 100% 동일한 규격 적용)**:
  1. **'전체' 탭 추가 및 통합 건수 표시**: 카테고리 탭 최좌측에 `[전체 (237)]` 탭을 추가하여 모든 기사를 한눈에 모아볼 수 있는 기본 뷰 제공.
  2. **개별 카테고리 클릭 시 단독 렌더링 필터링**: `도로·교량·철도`, `수자원·하천·항만` 등 원하는 카테고리를 클릭하면 다른 카테고리는 즉시 숨겨지고 **오직 선택된 카테고리의 섹션과 기사 카드들만 화면에 깔끔하게 단독 렌더링**.
  3. **결과 건수 안내 및 반응형 스크롤 보정**: 선택된 카테고리명과 기사 건수가 상단 안내 바에 즉시 동기화되며, 긴 스크롤 상태에서 탭을 누르면 상단 탭 네비게이션 위치로 매끄럽게 자동 스크롤 정렬.
  4. **카테고리 내 검색 지원**: 특정 카테고리가 선택된 상태에서 검색 또는 키워드 칩 필터링 시 해당 카테고리 내부에서 집중 검색되도록 연계.

#### 20) Google Gemini 1.5 Flash + Local RAG 토목 뉴스 브리핑 AI 챗봇 탑재 (ver 1.0.19)
- **요청 사항**: "챗봇을 넣고 싶은데 내가 토목 뉴스 페이지에서 기사에 관련된걸 질문하면 답해주는 기능을 가진 챗봇을 만들고 싶은데 ... 2번 방안으로 진행해줘"
- **구현 방식 (방안 2: Google Gemini 1.5 Flash API + 실시간 In-context RAG)**:
  1. **플로팅 트리거 UI (`#chatbotFloatingBtn`)**:
     - 화면 우측 하단에 에메랄드 펄스 애니메이션과 스파클 아이콘이 적용된 플로팅 캡슐 버튼 배치 (모바일 하단 네비게이션 간섭을 방지하도록 `bottom-20` 최적화).
     - 클릭 시 부드러운 스케일/페이드 애니메이션과 함께 대화형 AI 챗봇 모달 팝업.
  2. **하이브리드 RAG (Retrieval-Augmented Generation) 엔진**:
     - 사용자가 질문을 입력하거나 추천 칩을 누르면, 현재 서비스 중인 237개 기사 데이터셋(`allArticles`)에서 제목, 3줄 요약, 본문, 카테고리를 토큰 기반 다차원 가중치로 실시간 스코어링하여 가장 연관성 높은 상위 4개 기사를 신속 추출.
     - 기사 원문 링크, 언론사명, 발행일자 정보를 답변 하단 인용 출처 칩(`renderSourceChips`)으로 제공하여 팩트 원문 직접 확인 가능.
  3. **Google Gemini 1.5 Flash API 클라이언트 연동**:
     - 브라우저 Local Storage에 저장된 개인 API 키(`civil_gemini_api_key`)를 기반으로 Google AI Studio Gemini 1.5 Flash 엔드포인트 실시간 통신.
     - 토목·인프라 엔지니어링 전문 AI 연구원 시스템 프롬프트 주입을 통해 수치(사업비, 완공 일정, 공법 등) 중심의 팩트 기반 요약 및 정중한 불릿 스타일 답변 생성.
  4. **API 키 미등록 시 정밀 로컬 RAG 팩트 브리핑 폴백(Fallback)**:
     - API 키가 아직 설정되지 않은 방문자도 질문 즉시 관련 기사의 핵심 요약 불릿과 원문 링크 카드를 제공받아 챗봇이 100% 정상 작동하도록 무장애 폴백 설계.
     - 상단 🔑 설정 서랍에서 1-클릭으로 Google AI Studio 무료 키 발급 페이지로 이동하여 안전하게 키를 등록/삭제할 수 있는 UI 제공.
  5. **원터치 추천 질문 칩 제공**:
     - `🚇 GTX-A 개통 및 예산은?`, `🛡️ 지하안전평가 개정 핵심`, `✈️ 가덕도 신공항 입찰 현황`, `🏗️ 2026 스마트건설 동향` 등 자주 묻는 질문 칩을 상단에 배치하여 터치 한 번으로 즉시 답변 청취 가능.

#### 21) JCloud Ubuntu 24시간 서비스 서버 및 Flask·Nginx 무중단 운영 환경 구축 (ver 1.0.20)
- **요청 사항**: 정적 GitHub Pages의 한계를 극복하고 JCloud 우분투 서버를 24시간 풀 서비스 서버로 구축 (Flask + Gunicorn/systemd, Gemini API 키 서버 프록시, Nginx 리버스 프록시, 매일 07:00 크롤링 cron).
- **구현 내역**:
  1. **Python Flask 백엔드 구축 및 Gunicorn + systemd 24시간 무중단 백그라운드 구동**:
     - `requirements.txt`에 `flask`, `gunicorn`, `python-dotenv`, `requests` 패키지 명시 및 `venv` 환경 구축.
     - `app.py`를 Flask 마이크로서비스로 전면 개편하고, systemd 데몬(`civil-news-hub.service`)을 등록하여 서버 부팅 시 자동 시작 및 장애 시 3초 내 자동 재시작(`Restart=always`) 보장.
  2. **Google Gemini 1.5 Flash API 서버 프록시 연동 (`/api/chat`, `/api/chat/status`)**:
     - 서버 `.env` 환경변수에 `GEMINI_API_KEY`를 설정하여, 방문자가 브라우저에 직접 키를 넣지 않아도 서버 백엔드를 통해 누구나 100% 무료로 AI 브리핑 답변을 이용할 수 있도록 구축.
     - 클라이언트 `chatbot.js`에서 서버 키 존재 시 "Gemini 1.5 (서버 무료 이용)" 뱃지 자동 점등 및 원활한 Q&A 지원 (개인 키 우선 적용 및 GitHub Pages 정적 환경 클라이언트 폴백 완벽 호환).
  3. **Nginx 1.24 고성능 리버스 프록시 웹 서버 구축**:
     - 포트 80(HTTP)을 통해 정적 웹 리소스(HTML, JS, CSS)를 직접 초고속 서빙(Gzip 압축 및 캐싱 적용).
     - `/api/` 요청은 내부 Gunicorn Flask 백엔드(`127.0.0.1:5000`)로 안전하게 리버스 프록시 연동.
     - Clean URL (`/jobs`, `/contests`, `/mobile`) 및 PWA 파일 무중단 서빙.
  4. **매일 아침 7시 토목 뉴스·공모전·채용 자동 크롤링 Cron 데몬 등록**:
     - 한국 표준시(KST) 매일 아침 07:00에 자동 실행되는 크롤링 파이프라인 스크립트(`cron_scrape.sh`) 작성 및 crontab 스케줄 등록 (`0 7 * * *`).
     - 뉴스(`scraper.py`)와 채용(`job_scraper.py`)을 순차 실행하여 24시간 최신 팩트 데이터베이스 자동 유지 및 실행 로그 기록.

#### 22) JCloud OpenStack 1:1 포트포워딩 네트워크 정밀 분석 및 접속 환경 최적화 (ver 1.0.21)
- **발생 문제 분석**:
  - JCloud OpenStack 대시보드에서 보안 그룹 생성 시도 시 정책 제한 오류(`NeutronError: rule:create_security_group is disallowed by policy`) 발생.
  - 외부 브라우저 접속 시 SSH 포트(`19131` / 내부 7777)로 잘못 접근하여 OpenSSH 데몬의 `banner exchange: invalid format` 거절 및 브라우저 연결 오류 발생.
- **해결 및 규격 동기화 내역**:
  1. **JCloud 1:1 NAT 포트포워딩 매핑 규격 정밀 반영 (`Project1.pdf` 가이드 준수)**:
     - 학생 계정은 별도 보안 그룹 생성/수정이 불필요하며, 공용 IP(`113.198.66.75`)와 내부 IP(`10.0.0.131`, `xxx=131`) 간에 이미 사전 포트포워딩이 활성화되어 있음을 규명.
     - 웹 브라우저 접속 주소: **`http://113.198.66.75:18131`** (내부 HTTP 80번 연동) 및 **`http://113.198.66.75:10131`** (내부 8080번 연동).
     - SSH 접속 주소: **`113.198.66.75:19131`** (내부 7777번 연동).
  2. **VS Code 포트포워딩(SSH 터널링) 최적 접속 대안 구축**:
     - Remote-SSH 환경의 `PORTS` 탭을 통해 포트 8080/80을 로컬(`http://localhost:8080`)로 터널링하여 외부망/교내망 제약 없이 100% 접속 보장.
  3. **서버 Gemini API 연동 챗봇 안정화 및 UI 개선**:
     - 서버 사이드 Gemini 3.6 Flash 모델 연동 정상화 (`/api/chat` 테스트 통과 및 고품질 한국어 응답 생성).
     - 클라이언트 챗봇 뱃지를 "Gemini (서버 무료 이용)"으로 정돈.

#### 23) 챗봇 하단 입력바 완전 고정, 대화 내역 독립 스크롤 및 모바일 배경 스크롤 락 구현 (ver 1.0.22)
- **요청 사항**:
  1. 챗봇 창 크기는 현재 규격 그대로 완전 유지.
  2. 하단 텍스트 입력창(`chatbotInputContainer`)을 창 맨 아래에 흔들림 없이 완전 고정.
  3. 주고받은 채팅 내역들(`chatbotMessagesContainer`)이 잘리지 않고 독립적으로 부드럽게 스크롤되도록 보장.
  4. 모바일 환경에서 챗봇을 열었을 때 뒷배경 페이지가 멋대로 스크롤되지 않도록 완벽 락(Lock) 처리.
- **구현 및 최적화 내역**:
  1. **플렉스박스 레이아웃 계층 완전 정돈 및 입력바 하단 고정**:
     - `#chatbotWindow` 활성화 시 `display: flex !important; flex-direction: column !important;` 강제 적용.
     - 하단 입력 컨테이너(`chatbotInputContainer`)에 `flex-shrink: 0 !important; margin-top: auto !important;`를 부여하여 메시지가 아무리 길어져도 항상 창 맨 밑바닥에 밀림 없이 100% 고정.
  2. **대화 내역(`chatbotMessagesContainer`) 독립 스크롤 및 오버스크롤 차단**:
     - 메시지 컨테이너에 `flex: 1 1 0% !important; min-height: 0 !important; overflow-y: auto !important;` 적용하여 잔여 높이를 정밀 계산하여 독립 스크롤 구현.
     - `overscroll-behavior: contain !important;` 및 `-webkit-overflow-scrolling: touch;`를 부여하여 모바일 터치 스와이프 시 스크롤 끝단에서 바깥 페이지로 스크롤 이벤트가 전파(Scroll Chaining)되는 현상 원천 차단.
     - 가독성 높은 슬림 스크롤바 디자인 적용.
  3. **모바일 배경 페이지 스크롤 완벽 락 (Body Scroll Lock)**:
     - 챗봇 오픈 시(`toggleChatbotWindow(true)`), 뷰포트 너비 768px 미만 모바일 환경에서 현재 `scrollY` 위치를 메모리에 보관하고 `body`에 `position: fixed; width: 100%; top: -${scrollY}px; overflow: hidden;` 동적 주입.
     - 챗봇 닫힘 시(`toggleChatbotWindow(false)`), 원래의 스크롤 위치(`scrollY`)로 오차 없이 완벽 복원하여 화면 튕김 현상 방지.
     - 모바일 전용 딤 배경(`chatbotBackdrop`)을 추가하여 바깥 터치 시 스크롤 차단(`touchmove.preventDefault()`) 및 부드러운 원터치 닫기 지원.
     - ESC 키 입력 및 창 리사이즈 대응 추가.

#### 24) 모바일 외부망 접속 불가 원인 분석 및 24시간 보안 HTTPS 터널링(civil-tunnel.service) 구축 (ver 1.0.23)
- **발생 문제 분석**:
  1. JCloud 인스턴스 보안 그룹이 학생 권한으로 잠겨 있어 80/8080 인바운드 규칙 등록 불가 (`PolicyNotAuthorized`).
  2. 전북대 학내망 방화벽에서 외부 공용망(스마트폰 LTE/5G)발 80/8080 포트 직접 인바운드 접근 차단.
  3. 모바일 브라우저(사파리, 크롬)의 HTTPS 강제 정책으로 인한 SSL 인증서 부재 에러.
- **해결 및 구축 내역**:
  1. **24시간 고성능 보안 HTTPS 터널링 구축 (`civil-tunnel.service`)**:
     - `cloudflared` (HTTP/2 프로토콜) 기반으로 Cloudflare 서울 엣지 데이터센터(`icn05`)와 1:1 암호화 아웃바운드 터널 연결.
     - JCloud 보안 그룹/방화벽 설정 변경 없이 어디서나(외부 LTE, 5G, Wi-Fi) 접속 가능한 정식 보안 HTTPS URL 생성.
     - systemd 서비스(`civil-tunnel.service`)로 등록하여 24시간 무중단 자동 유지 (`Restart=always`).
  2. **모바일 브라우저 및 Gemini AI 연동 검증**:
     - 모바일 HTTPS 요청(`/api/chat/status`, `/api/chat`)을 Nginx를 거쳐 내부 Gunicorn Flask 및 Google Gemini 3.6 Flash로 100% 정상 연동 검증.

#### 25) 챗봇 창 모바일 화면 밑바닥 밀착 고정(Bottom Sheet) 및 리로드 시 좌표 튐·스크롤 이탈 원천 수정 (ver 1.0.24)
- **발생 문제 분석**:
  1. `lockBodyScroll()` 실행 시 `document.body`에 `position: fixed; top: -${scrollY}px;`를 적용하여, 모바일 뷰포트에서 고정(fixed)된 챗봇 창의 기준 좌표가 스크롤 오프셋만큼 위로 밀려 화면 하단에 고정되지 않고 공중에 붕 뜨는 현상 발생.
  2. 페이지 새로고침(리로드) 또는 모바일 당겨서 새로고침 시 Service Worker의 과거 캐시(`v1.0.20`) 및 스크롤 복원 오차로 인해 채팅창 위치와 대화 스크롤이 위로 튀는 현상 발생.
- **해결 및 최적화 내역**:
  1. **모바일 챗봇 창 바닥 완전 밀착(Bottom Sheet) 규격 개편**:
     - 모바일(640px 미만) 환경에서 `inset-x-0 bottom-0 w-full rounded-t-3xl rounded-b-none`으로 화면 맨 밑바닥에 완전 밀착.
     - 하단 입력창에 `pb-[max(0.75rem,env(safe-area-inset-bottom))]`를 주입하여 갤럭시·아이폰 제스처 바 간섭 완벽 차단.
     - 데스크탑 환경은 기존 우하단 플로팅 규격(`sm:bottom-6 sm:right-6 sm:w-[440px] sm:h-[620px] sm:rounded-3xl`) 100% 유지.
  2. **바디 락 좌표 왜곡 제거 (Clean Viewport Lock)**:
     - `body`의 `position: fixed` 및 `top` 조작을 전면 제거하고, `html.chatbot-open-lock`, `body.chatbot-open-lock` 클래스로 `overflow: hidden; touch-action: none; overscroll-behavior: none;`을 적용하여 뒷배경 스크롤은 완벽 차단하되 챗봇 창의 뷰포트 바닥 좌표가 절대 위로 튀지 않도록 수정.
  3. **세션 스토리지 기반 대화 내역 & 오픈 상태 복원 및 멀티프레임 자동 스크롤**:
     - 새로고침 시에도 대화 내역이 유실되지 않고 이전 대화 끝(하단)으로 즉시 복귀할 수 있도록 `sessionStorage` 연동.
     - `scrollToBottom()`을 멀티프레임(`requestAnimationFrame`, `setTimeout 80ms`, `250ms`)으로 강화하여 DOM 렌더링 완료 후 100% 최신 메시지 하단에 정렬.
  4. **PWA Service Worker 캐시 갱신 (`civil-news-hub-v1.0.24`)**:
     - `sw.js` 캐시명을 신규 버전으로 갱신하여 클라이언트 브라우저의 구버전 캐시 강제 삭제 및 최신 코드 자동 반영.

#### 26) 모바일 하단 네비게이션 바(토목 뉴스·채용 공고·공모전·마이페이지) 복구 및 챗봇 자동 오픈 차단 (ver 1.0.25)
- **발생 문제 분석**:
  1. 직전 작업에서 새로고침 시 챗봇 상태 복원을 위해 `civil_chatbot_open`을 `sessionStorage`에 저장하고 로드 시 자동 오픈(`toggleChatbotWindow(true)`)하도록 설정함.
  2. 이로 인해 사용자가 새로고침하거나 사이트에 재접속할 때마다 챗봇 바텀 시트(`h-[85vh]`, `z-50`, `bottom-0`)가 화면 맨 아래에 강제 팝업되며, 기존 모바일 전용 하단 고정 네비게이션 바(`z-40`, `bottom-0`)를 완전히 덮어씌워 하단 UI가 사라진 것처럼 보이는 현상 발생.
  3. 모바일 하단 네비게이션 버튼 텍스트가 `뉴스`, `채용` 등 약칭으로 되어 있어 데스크탑 및 시뮬레이터 뷰어의 정식 명칭과 불일치.
- **해결 및 최적화 내역**:
  1. **페이지 로드/새로고침 시 챗봇 자동 오픈 전면 차단**:
     - `chatbot.js` 및 `static/chatbot.js`의 `loadChatSession()`에서 `sessionStorage.removeItem('civil_chatbot_open')`을 적용하여 새로고침 시 챗봇이 멋대로 열려 화면 및 하단 네비게이션 바를 가리는 현상을 원천 방지함.
     - 대화 내역(`civil_chatbot_history`)은 안전하게 유지하여 사용자가 우하단 플로팅 버튼(`AI 기사 질문`)을 눌렀을 때만 이전 대화가 즉각 렌더링되도록 구현.
  2. **모바일 하단 네비게이션 바(`#mobileBottomNav`) 복구 및 표준 명칭 동기화**:
     - `index.html` 및 `static/index.html`의 모바일 네비게이션 바에 `#mobileBottomNav` ID 부여 및 명시적 CSS(`display: flex !important; position: fixed !important; bottom: 0 !important; z-index: 40 !important;`)를 `style.css`에 추가하여 레이아웃 충돌 원천 방지.
     - 4대 탭 명칭을 사용자가 요청한 정식 표준 명칭인 **`토목 뉴스`**, **`채용 공고`**, **`공모전`**, **`마이페이지`**로 100% 일치시키고 `flex-1` 균등 4분할 및 모바일 제스처 바 안전 영역(`pb-[max(0.5rem,env(safe-area-inset-bottom))]`) 적용.
     - `app.js`의 `updateMobileNavStyles()` 내 `flex-1` 유지 적용.
  3. **PWA Service Worker 캐시 갱신 (`civil-news-hub-v1.0.25`)**:
     - 캐시 버스팅 파라미터(`?v=20260911_0630`) 갱신 및 캐시 키 교체로 클라이언트 기기에 즉시 반영.

#### 27) 뷰포트 맞춤 동적 스케일링, 모바일 하단 네비게이션 바 상시 노출 및 캐시 방지 전면화 (ver 1.0.26)
- **발생 문제 분석**:
  1. **데스크탑 모바일 시뮬레이터(`/mobile`) 화면 하단 잘림(Clipping)**:
     - 고정 915px 높이로 렌더링되던 갤럭시 프레임이 데스크탑/노트북(1080p 해상도, 가용 높이 약 800~900px)에서 화면 하단 밖으로 150~200px 밀려나며, 프레임 하단의 제스처 바 및 `#mobileBottomNav`가 모니터 밖으로 잘림.
     - iframe 내부로 마우스 휠 이벤트가 포커싱되어 외부 페이지 스크롤이 불가능하여 사용자가 하단바를 볼 수 없었음.
  2. **모바일 브라우저 PWA Service Worker 및 Nginx 캐시 지속(Stale SW Cache)**:
     - Android 크롬 등 실제 모바일 기기에서 PWA Service Worker가 `index.html`을 캐시 우선(Cache-First)으로 서빙하여, 로컬 서버 재접속 시 이전 버전 캐시가 표시되며 하단바 갱신 내역이 반영되지 않는 현상 발생.
  3. **반응형 브레이크포인트(640px vs 768px) 제한**:
     - 기존 `sm:hidden`(640px 기준)으로 설정되어 있어 작은 창 모드나 프리뷰 창에서 640~767px 사이 구간일 때 하단바가 숨겨지는 문제.
- **해결 및 최적화 내역**:
  1. **모바일 시뮬레이터 뷰포트 맞춤 동적 스케일링 (`fitPhoneToViewport`)**:
     - `mobile.html` 및 `static/mobile.html`에 브라우저 높이(`window.innerHeight`)와 상단 툴바, 하단 안내를 계산하여 화면 높이에 맞춰 프레임을 자동 축소하는 `fitPhoneToViewport()` 동적 스케일링(`transform: scale(...)`, `transformOrigin: top center`, `marginBottom` 오프셋) 구현.
     - 어떤 모니터, 노트북 해상도에서도 상단 펀치홀 카메라부터 최하단 `#mobileBottomNav` 및 제스처 바까지 100% 한 화면에 잘림 없이 노출.
     - `resize` 및 `load` 이벤트에 바인딩하여 창 크기 조절 시 즉시 반응.
  2. **모바일 네비게이션 브레이크포인트 표준화 (`md:` 768px)**:
     - `index.html`, `static/index.html`의 `#mobileBottomNav`를 `md:hidden`으로, 데스크탑 헤더를 `hidden md:block`으로 변경.
     - `style.css`의 명시적 미디어 쿼리를 `@media (max-width: 767px)`로 상향하여 태블릿 세로 모드 및 모든 스마트폰 기기에서 상시 노출 보장.
     - 푸터 영역에 `pb-24 md:pb-6`를 적용하여 스크롤 최하단 도달 시 하단바에 의한 콘텐츠 가림 현상 방지.
  3. **Service Worker Network-First 전략 개편 및 캐시 무효화 (`civil-news-hub-v1.0.26`)**:
     - `sw.js` 및 `static/sw.js`에서 네비게이션 및 HTML 요청(`event.request.mode === 'navigate'`, `*.html`, `/`, `/mobile`)에 대해 **Network First** 전략을 적용하여 새로고침 시 항상 서버의 최신 DOM 및 스크립트가 즉시 반영되도록 구현 (오프라인 시에만 캐시 폴백).
     - `CACHE_NAME`을 `civil-news-hub-v1.0.26`으로 갱신하여 기존 캐시 완전 제거.
  4. **Nginx HTML 실시간 no-cache 헤더 전면 적용**:
     - `/`, `/mobile`, `*.html` 라우트에 `Cache-Control: no-cache, no-store, must-revalidate` 및 `Pragma: no-cache`를 설정하고 nginx 재로드 완료.
     - 모든 스크립트/CSS 로드 쿼리스트링 파라미터를 `?v=20260911_1935`로 일괄 동기화.

#### 28) DOM 태그 불균형(미닫힘 div) 완전문맥 교정 및 모바일 하단바 완전 노출 보장 (ver 1.0.27)
- **발생 문제 분석**:
  1. **`<div id="contestDetailModal">` 태그 미닫힘 버그 (근본 원인 적발)**:
     - `index.html` 및 `static/index.html`의 793행에서 공모전 상세 모달(`contestDetailModal`)의 최상단 wrapper `<div>`를 닫는 `</div>` 태그가 누락되어 있었음.
     - 이로 인해 이후에 선언된 캘린더 모달, 북마크 메모 모달, 푸터, 그리고 **`#mobileBottomNav`**가 모두 `contestDetailModal`의 하위 자식 노드로 DOM 트리에 파싱됨.
     - `contestDetailModal`이 기본적으로 `invisible opacity-0`(즉 `visibility: hidden; opacity: 0;`) 스타일을 가지므로, 자식으로 갇힌 `#mobileBottomNav` 역시 CSS 상속으로 인해 **화면 전체에서 100% 영구적으로 투명화·비가시화(`invisible`)**되어 모바일 기기 및 시뮬레이터에서 전혀 보이지 않는 현상의 원천 원인이었음.
  2. **검색 입력창 self-closing 태그 누락 교정**:
     - `newsSearchInput`에 닫는 슬래시(`/>`)가 누락되어 일부 엄격한 파서에서 검색 초기화 버튼이 씹히는 문제 교정.
- **해결 및 최적화 내역**:
  1. **DOM 트리 태그 밸런스 100% 정상화**:
     - `contestDetailModal` 직후 누락되었던 닫는 `</div>`를 보강 삽입하여 Python 파서 기준 **Unclosed tags: 0 (All clean!)** 검증 완료.
     - 푸터 및 `#mobileBottomNav`가 정상적으로 `<body>`의 독립된 직계 자식 노드로 분리되어 모달의 `invisible` 간섭을 100% 탈피함.
  2. **PWA Service Worker 캐시 갱신 (`civil-news-hub-v1.0.27`)**:
     - 캐시 버스팅 파라미터(`?v=20260911_1947`) 및 `sw.js` 캐시 키를 일괄 갱신하여 클라이언트 기기에 즉각 반영.

#### 29) 취업 대비 포트폴리오 노션(Notion) 완전 자동 업로드 파이프라인 구축 (ver 1.0.28)
- **배경 및 요구사항**:
  - 개발자가 지금까지 구축한 서비스 기획, 아키텍처, 트러블슈팅(STAR 기법) 경험을 취업 포트폴리오로 활용하기 위해 개인 노션 페이지(`https://app.notion.com/p/3d5009a14e198038ba3ff62b74d18e03`)에 자동 업로드 연동 요청.
- **구현 및 자동화 내역**:
  1. **종합 기술 포트폴리오 문서화 ([`NOTION_PORTFOLIO_GUIDE.md`](file:///home/ubuntu/workspace/NOTION_PORTFOLIO_GUIDE.md))**:
     - 1인 개발 역할 및 기여도 100%, Mermaid 아키텍처 다이어그램, 10대 기술 스택 선정 이유, 6대 핵심 기능, 6대 핵심 문제 해결 사례(STAR) 총망라.
  2. **Notion 공식 API 자동 변환 스크립트 ([`sync_to_notion.py`](file:///home/ubuntu/workspace/sync_to_notion.py)) 개발**:
     - 마크다운의 헤딩, 콜아웃, 코드 블록, 인라인 서식(볼드, 링크, 인라인 코드) 및 2개 대형 데이터 표(Table)를 노션 API 규격 블록으로 100% 무손실 자동 파싱 (총 123개 블록).
     - Notion API의 1회 전송 100개 블록 제한을 안전하게 분할 전송(청크 크기 40개)하여 안정적인 무오류 업로드 구현.
  3. **GitHub Actions 자동 동기화 워크플로우 ([`.github/workflows/sync_notion.yml`](file:///home/ubuntu/workspace/.github/workflows/sync_notion.yml)) 구축**:
     - GitHub Secrets(`NOTION_TOKEN`) 연동을 통해, 포트폴리오 문서 수정 후 `git push` 시 또는 웹에서 'Run workflow' 클릭 한 번으로 사용자의 노션 페이지에 최신 포트폴리오가 100% 전자동으로 배포되도록 구성.

#### 30) 사용자 혼선 방지를 위한 '최신 수집' 버튼 UI 완전 제거 (ver 1.0.29)
- **배경 및 원인 분석**:
  - 헤더 우측의 `[최신 수집]` 버튼은 클라이언트 JS에서 이미 서버에 있는 정적 json 파일을 단순 re-fetch하는 역할만 수행하여, 사용자가 기대하는 실시간 신규 웹 크롤링 실행이 즉각 일어나지 않아 사용자에게 불필요한 혼선과 의문을 유발함.
  - 실제 데이터 크롤링은 GitHub Actions(`daily_crawl.yml`)를 통해 매일 아침 07:00 KST에 신선한 상태로 자동 갱신 및 무중단 배포되므로, 수동 수집 버튼의 실효성이 없음.
- **해결 조치**:
  - `index.html` 및 `static/index.html` 상단 헤더 액션 영역에서 `#refreshBtn`('최신 수집') 버튼 요소를 전면 삭제하여 헤더 레이아웃을 간결하고 직관적으로 정돈함.

#### 31) 뉴스 카드 푸터 '원문' 버튼 제거 및 미니멀 카드 레이아웃 정돈 (ver 1.0.30)
- **배경 및 원인 분석**:
  - 뉴스 카드 헤드라인 제목 자체가 원문 기사로 직접 연결되는 엑스트라 볼드 딥링크 역할을 수행하고 있으므로, 카드 우측 하단 푸터 영역의 `[원문]` 버튼이 시각적 중복을 형성함.
  - 사용자 피드백에 따라 불필요한 중복 요소를 배제하고 카드 푸터의 여백을 시원하게 확보하기 위해 `[원문]` 버튼 삭제 결정.
- **해결 조치**:
  - `static/app.js`의 `renderArticleCard`에서 하단 `[원문]` 이동 앵커 버튼을 전면 제거함.
  - 카드 하단 우측에는 `[기사 공유]` 및 `[북마크 리본]` 버튼 2개만 단정하게 나란히 배치하여 모바일 및 PC 가독성을 극대화함 (기사 원문 읽기는 기사 헤드라인 제목 클릭을 통해 완벽 유지).

#### 32) '폰으로 보기' QR 모달 실시간 스마트폰 라이브 터널 URL 연동 (ver 1.0.31)
- **배경 및 요구사항**:
  - 기존 '폰으로 보기' 모달은 공유기 내부 사설 IP(`http://192.168.25.58:8000/#news`)를 하드코딩하여, 동일 Wi-Fi가 아닌 LTE/5G 환경에서는 스마트폰 접속이 불가능하고 주소가 불일치하는 문제가 존재함.
  - 사용자가 현재 스마트폰에서 실제로 사용하는 실시간 라이브 URL(`https://diverse-tattoo-exterior-reporting.trycloudflare.com/#news`)로 '폰으로 보기' 모달의 QR 코드와 복사 주소를 최신화하도록 요청.
- **해결 조치**:
  - **백엔드 동적 터널 감지 (`app.py`)**: `civil-tunnel.log`에서 활성 터널 URL을 동적으로 파싱하는 `get_live_tunnel_url()` 함수를 구현하고, `/api/network-info` 엔드포인트가 실시간 라이브 터널 URL을 자동 반환하도록 개선.
  - **프론트엔드 모달 UI 전면 갱신 (`index.html`, `mobile.html`, `static/index.html`, `static/mobile.html`)**:
    - 모달 제목을 '스마트폰에서 바로 열기'로 개편하고, 실시간 터널 주소로 QR 코드 및 복사 텍스트 기본값 지정.
    - 과거 사설 Wi-Fi 제약 안내를 '24시간 암호화 라이브 터널 연결됨 (LTE/5G 환경에서도 즉시 접속 가능)' 안내 및 공식 GitHub 배포 링크로 개선.
  - **클립보드 복사 폴백 갱신 (`static/app.js`, `mobile.html`, `static/mobile.html`)**: API 응답 실패 시에도 라이브 터널 주소가 기본 복사되도록 안전하게 동기화.
  - **서비스 워커 및 캐시 버스팅 일괄 갱신**: `sw.js`의 `CACHE_NAME`을 `civil-news-hub-v1.0.31`로 갱신하고 스크립트/CSS 로드 쿼리스트링(`?v=20260911_2140`)을 일괄 적용.

---

## 🌿 3. Git 브랜치 현황

| 브랜치명 | 상태 | 설명 |
| :--- | :--- | :--- |
| **`feature/footer-last-updated`** | **최신 작업 완료 (원격 푸시됨)** | 푸터 업데이트 이전, 글씨 늘어남 차단, 상단 뱃지 및 설명 문구 삭제가 모두 반영된 최신 브랜치 |
| **`main`** | **공식 배포 브랜치** | GitHub Pages를 통해 라이브 서비스되는 기준 브랜치 (언제든 `feature/footer-last-updated`와 병합 가능) |

- **온라인 라이브 서비스**: [https://chldlrtjr.github.io/civil-news-hub/](https://chldlrtjr.github.io/civil-news-hub/)
- **로컬 실행 주소**: `http://localhost:8000` (모바일 시뮬레이터: `http://localhost:8000/mobile`)

---

## 🚀 4. 향후 로드맵 (Future Milestones)

1. **주간 인기 토목 뉴스 TOP 5 랭킹 위젯**: 메인 화면 상단에 한 주간 가장 조회수가 높고 화제가 된 토목 이슈 TOP 5 시각화
2. **관심 전문분야 맞춤 큐레이션 필터**: 수자원, 구조, 지반, 도로·철도, 스마트건설 중 선호 분야를 체크하면 해당 정보만 집중 피드로 제공
3. **2026 토목 기술 자격증 & 시험 일정 캘린더**: 토목기사, 토목구조기술사, 토질및기초기술사 등 국가공인 자격시험 일정 D-Day 및 접수 알림
